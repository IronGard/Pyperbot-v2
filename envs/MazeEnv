import argparse
import os
import time


class Pyperbot:
    def __init__(self, length, base_position, base_orientation, mode = "position"):
        '''
        Args:
            length (int): number of joints
            client (bc): pybullet client interface
            base_position (array): cartesian coordinates of snake in x, y, z axes
            base_orientation (array): RPY of robot
            mode: joint interface mode
        '''

    def generate_sidewinding_movement(self):